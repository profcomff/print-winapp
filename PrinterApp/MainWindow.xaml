<Window x:Class="PrinterApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrinterApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="200" Width="400" WindowStyle="None" ResizeMode="NoResize" Topmost="True" Closed="MainWindow_OnClosed">
    <Grid Background="{StaticResource Brush1}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBox x:Name="Code" Grid.Row="0" Margin="20" PreviewTextInput="TextBox_OnPreviewTextInput"
                 DataObject.Pasting="TextBoxPasting" PreviewKeyDown="TextBox_OnKeyDown" FontSize="32"
                 FontFamily="Source Code Pro"
                 TextAlignment="Center" Background="{StaticResource Brush2}" />
        <Button Grid.Row="1" Margin="30" Content="Печать" Click="Print_OnClick" Background="{StaticResource Brush2}"
                FontSize="24" FontFamily="Source Sans Pro" />
        <TextBlock x:Name="ErrorBlock" Grid.Row="1" Margin="30 0 30 70" FontSize="24" FontFamily="Source Sans Pro"
                   TextAlignment="Center" Foreground="DarkRed" />
        <ProgressBar x:Name="ProgressBar" Grid.Row="1" Margin="30" Visibility="Collapsed" />
    </Grid>
</Window>